*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Vibe service (deployed separately from R2)
vibe-service/
public/vibe/
.superpowers/
.claude/scheduled_tasks.lock

# NativePHP — entirely generated by `native:install` / `native:run`
# (iOS/Android projects, downloaded PHP binaries, CocoaPods, Swift Package
# checkouts like ZIPFoundation). Reproducible build output; never version it.
/nativephp
.vite

# Internal planning/design docs — keep local, not in the public repo
/docs/superpowers/
